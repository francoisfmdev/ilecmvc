<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>{{h1}}</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="offset-10 col-2">
                <p>{{username}}</p>
            </div>
        </div>
        <div class="row">
            <div class="col-6">
                <ul>
                    {% for task in tasks %}
                        <li ><p style="{{ task.status == 0 ? 'color:red' : 'color:green' }}">{{ task.content }}</p>
                           <span>
                              <form method="POST" action="/ilecmvc/change_status">
                                 <input type="hidden" name="task_id" value="{{ task.id }}">
                                 <input type="hidden" name="status" value="{{ task.status }}">
                                 <button class="btn btn-sm btn-warning" type="submit">change status</button>
                              </form>
                           </span> 
                        </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="col-6">
                <form method="POST" action="/ilecmvc/add_task">
                    <div class="mt-3">
                       <label for="content">Contenu de la tache</label>
                       <input class="form-control" type="text" id="content" name="content" required>
                    </div>
                    <div class="mt-3">
                        <button class="btn btn-primary" type="submit">Ajouter une tâche</button>
                    </div>
                </form>
            </div>
            
        </div>
    </div>
   
</body>
</html>